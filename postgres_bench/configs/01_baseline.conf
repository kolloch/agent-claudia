# Baseline: PostgreSQL defaults (no performance tuning).
# Used as the reference point for comparison.
#
# No overrides — initdb defaults apply (shared_buffers=128MB,
# fsync=on, synchronous_commit=on, full_page_writes=on).
