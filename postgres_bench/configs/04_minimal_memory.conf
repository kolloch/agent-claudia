# Minimal memory: reduces shared buffers, working memory, and
# maintenance memory to the smallest values that still allow
# basic operations.  Useful for memory-constrained environments.

shared_buffers          = 8MB
work_mem                = 1MB
maintenance_work_mem    = 16MB
temp_buffers            = 1MB
