# No durability: disables fsync, synchronous commit, and full-page
# writes entirely.  The OS page cache handles all I/O.
# Risk: database corruption on OS crash (acceptable for ephemeral
# test instances that are thrown away after each run).

fsync                = off
synchronous_commit   = off
full_page_writes     = off
