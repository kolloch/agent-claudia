# Async commit: commits return before WAL is flushed to disk.
# Risk: up to ~0.6 s of committed transactions may be lost on crash.
# This is often the safest durability trade-off — no corruption, just
# minor data loss. Suitable for most testing workloads.

synchronous_commit = off
